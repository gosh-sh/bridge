# Questions (QC)

| ID | Topic | Context | Status |
|----|-------|---------|--------|
| QC-A1-1 | `MAX_DEPOSIT_AMOUNT` — per-call, не per-address/global | `AckiNackiBridge.sol` L59: комментарий «prevents whale deposits», но cap 100 USDC обходится дроблением на N вызовов `deposit` — суммарный TVL/exposure не ограничен. Это осознанный per-tx limit или нужен агрегатный cap для milestone'а? (A1-F2) | open |
| QC-A1-2 | Trust-модель USDC (upgradeable proxy, blacklist/pause Circle) | `deposit` L587 (`transferFrom`) и `treasuryBalance += amount` L592 предполагают токен без fee-on-transfer; mainnet USDC — прокси, апгрейд может это сломать (скрытая неплатёжеспособность, убыток последним выводящим). Blacklist бриджа Circle'ом = полная заморозка. Зафиксировать assumption в `audit/PROJECT_FACTS.md`? (A1-F3) | open |
| QC-A1-3 | `emergencyWithdrawAll` — судьба yield и недостачи AAVE | L1136–1149: (а) yield приходит на контракт liquid-USDC и после `suppliedPrincipal = 0` навсегда заперт (не harvest'ится, payout ограничен `treasuryBalance`); (б) при `received < principal` (недостача AAVE) `treasuryBalance` не корректируется — убыток ложится на последних выводящих без социализации. Принято как residual risk? (A1-F5) | open |
| QC-A2-1 | Дока anchor-инварианта устарела относительно кода (AB-Q4) | `bridge_verification.md` LH-3, CC-6, §5.3, §6.5, L6-мониторинг описывают `prevMaxLevelLayerHash == storedPrevMaxLevelLayerHash` (плоский top-layer), но `verifyBlock` L685–688 использует `_expectedPrevAnchor(numLayers)` = `_layerLatest(min(numLayers, t))`. На шагах уменьшения `numLayers` задокументированный L6-инвариант `prevMaxLevelLayerHash_in_event_t == storedPrevMaxLevelLayerHash(t-1)` ложно сработает. Обновить docs под per-layer pick; выверить мониторинг. (A2-F1) | open |
| QC-A2-2 | `applyBkSetUpdate` — связка курсоров `lastSeen` | L787/795 подают `storedLastSeenBlockSeqNo` (курсор `verifyBlock`) как публичный input `lastSeenBlockSeqNo` аттестации, но монотонность гейтится по отдельному `storedLastBkSetUpdateSeqNo` (L777). Если `verifyBlock` продвинул курсор между генерацией прува ротации и его отправкой — аттестация ротации будет отвергнута (liveness-связка). Какой `lastSeen` использует prover при генерации BK-update-прува? Требуется подтверждение партнёра. (A2-F2) | open |
| QC-A2-3 | Нулевой хеш в активном слое принимается | `verifyBlock` проверяет на ноль только хвост `i ≥ numLayers` (L669–671); активный слот с `layerHashes[i]==0` (`i<numLayers`) принимается. `_appendLayerHashes` L823 пропускает нулевые значения → слой не попадает в своё окно, что может рассинхронизировать `_highestActiveLayer`/`_expectedPrevAnchor` и множество якорей `withdrawByProof`. Может ли Circuit 2 в принципе выдать нулевой хеш для активного слоя? Если нет — задокументировать как предположение; если да — нужен контрактный чек `layerHashes[i]!=0` для `i<numLayers`. (A2-F3) | open |
| QC-A2-4 | `_highestActiveLayer` монотонно не убывает | `_highestActiveLayer()` L880–888 берёт максимальный слой с непустым окном; окна никогда не очищаются, поэтому `t` не сокращается даже если AN постоянно уменьшил число слоёв. При последующем регрове `pick=min(numLayers,t)` может указать на устаревший высокий root (последний раз обновлённый давно). Совпадает ли это с партнёрской `prev_max_level_layer_hash_for` при **постоянном** сокращении числа слоёв (а не временном)? (A2-F4) | open |
| QC-A1-4 | AC-6 сформулирован шире, чем выполняется | `bridge_verification.md` §9.1 AC-6: «All external interactions follow CEI: state mutation precedes external call» — `deposit` (L587: `transferFrom` до effects) и `emergencyWithdrawAll` (L1141: withdraw до `suppliedPrincipal = 0`) следуют паттерну «pull-then-account», а не букве AC-6. Код безопасен (общий `nonReentrant`), нужна правка формулировки инварианта в docs. (A1-F1) | open |
| QC-A2-1 | Дока vs код: per-layer anchor (AB-Q4) | `bridge_verification.md` LH-3/CC-6 описывает плоский `prevMaxLevelLayerHash == storedPrevMaxLevelLayerHash`; код использует `_expectedPrevAnchor(numLayers)`. L6-мониторинг по старой формулировке будет ложно срабатывать при shrink `numLayers`. | open |
| QC-A2-2 | Liveness: `applyBkSetUpdate` ↔ `verifyBlock` | `lastSeenBlockSeqNo` PI берётся из `storedLastSeenBlockSeqNo`, гейт — по `storedLastBkSetUpdateSeqNo`. Между генерацией и отправкой прува ротации `verifyBlock` может продвинуть курсор → отказ ротации. | open |
| QC-A2-3 | Нулевой хеш в активном слоте layerHashes | CC-7 проверяет только хвост (`i ≥ numLayers`); `layerHashes[i]==0` при `i < numLayers` принимается, `_appendLayerHashes` пропускает нули → возможный рассинхрон окна/якорей для withdraw. Зависит от Circuit-2. | open |
| QC-A2-4 | Монотонность `_highestActiveLayer` | `t` не сокращается при постоянном уменьшении числа слоёv AN; `_expectedPrevAnchor` может брать устаревший высокий root. Нужна партнёрская семантика. | open |
| QC-A4-1 | `ShplonkHalo2Verifier` — нет `extcodesize`-guard на Yul-verifier | `ShplonkHalo2Verifier.sol:21-23`: `(ok,) = yulVerifier.staticcall(...)` — `staticcall` на адрес **без кода** возвращает `ok == true`, т.е. пустой/EOA-адрес Yul-верификатора превращается в «все прувы валидны» (полный bypass `verifyBlock` / `applyBkSetUpdate` / `withdrawByProof`). Конструктор проверяет только `!= address(0)`. Сейчас закрыто deploy-путём (`ShplonkDeployLib.deployYulFromBin` отвергает пустой `.bin` и `create()==0`), но on-chain гарантии нет. Добавить `extcodesize > 0` в ctor (и/или в `verify`)? (A4-01) | open |
| A4-Q1 | SHPLONK Yul-верификатор — проверка наличия кода | `ShplonkHalo2Verifier.verify` (L22): `(ok,) = yulVerifier.staticcall(...)` трактует успех как «proof валиден». `staticcall` на адрес без кода/EOA возвращает `ok == true` ⇒ обход верификатора на `verifyBlock`/`applyBkSetUpdate`/`withdrawByProof`. Конструктор проверяет только `!= address(0)`, `extcodesize`-гарды нет. Смягчено `ShplonkDeployLib` (непустой `.bin` + успех `create`), но не рантаймом. Добавить `extcodesize(yulVerifier) > 0` в ctor обёртки и/или проверку кода верификаторов в конструкторе моста? (A4-01) | open |
| A4-Q2 | Governance паузы | Владелец может держать `pause()` бесконечно, замораживая `withdrawByProof` (кросс-чейн выплаты) и `deposit`. Нужен ли guardian-split / timelock / max-pause-duration до mainnet, чтобы скомпрометированный/неотзывчивый владелец не мог вечно цензурировать выводы? Средства не крадутся. (A4-02) | open |
| A4-Q3 | Учёт yield в `emergencyWithdrawAll` (дубль QC-A1-3а) | После `emergencyWithdrawAll` остаточный AAVE-yield попадает в liquid-USDC при `suppliedPrincipal = 0`, `accruedYield()` → 0, harvest невозможен (over-collateral казны). Так задумано или emergency должен слать yield на `yieldRecipient`? (A4-03) | open |

| WD-Q1 | Вытеснение якоря из L1-окна (`withdrawByProof` liveness) | `AckiNackiBridge.sol` L1042: `_isKnownLayerAnchor(1, pub.finalRoot)`. L1-окно — кольцевой буфер на 128 (`HISTORY_PROOF_WINDOW`); каждый `verifyBlock` пишет L1-хеш. `finalRoot` старше 128 блоков вытесняется ⇒ вывод навсегда reverts `UnknownAnchor`. Может ли prover перепривязать уже эмитированное withdrawal-событие к более свежему `finalRoot` (любому якорю, в который расширяется dense-chain)? Если да — это лишь неудобство ликвидности; если нет — медленный релеер/задержка отправки замораживает средства (в казне, не украдены). Нужно партнёрское подтверждение re-prove. (A3-01) | open |
| WD-Q2 | `recipient == address(0)` принимается | `AckiNackiBridge.sol` L1072/1093: `_reconstructRecipient` не отвергает нулевой адрес; `test_withdrawByProof_zeroRecipientWorks` фиксирует выплату на `address(0)`. Реальный USDC reverts перевод на 0 ⇒ tx откат, нуллификатор не расходуется, событие невыплачиваемо навсегда. Нужен ли ранний чек `recipient != address(0)` (revert `InvalidRecipient`), чтобы такое событие не «зависало», или это допустимый residual (AN-сторона не должна эмитить recipient=0)? (A3-02) | open |
| WD-Q3 | Groth16-адаптер `BridgeWithdrawalVerifier.sol` — R15 identity-stub | По NatSpec (L23-28) gnark-обёртка проверяет только identity-stub над PI, сам Halo2 SHPLONK-пруф НЕ верифицируется. Адаптер компилируется и деплоится из аудируемого набора файлов. Прод-скрипты (`DeployRealBridge`, `ShplonkDeployLib`) подключают `BridgeWithdrawalAggregatorVerifier` (реальный SHPLONK Yul). Подтвердить, что Groth16-stub-адаптер НИКОГДА не подключается в mainnet-деплое (Phase 8 blocker уже трекается) и зафиксировать deploy-инвариант/чек. (A3-03) | open |
| WD-Q4 | `WITHDRAW_ANCHOR_LAYER` жёстко = 1 | `AckiNackiBridge.sol` L75/L1042: якорный слой захардкожен под партнёрский witness builder (`layer_idx = 0`). Смена якорного слоя партнёром ⇒ все `withdrawByProof` fail-closed (`UnknownAnchor`) — безопасно, но полный halt ликвидности. Комментарий кода упоминает будущий PI-слот [10] `anchorLayer`. Планируется ли вынести `anchorLayer` в public input, или слой 1 зафиксирован контрактом деплоя? (A3-05) | open |

QC items stay open until partner/dev resolves; linked tests remain `xfail` with `QC-NN` in reason.
