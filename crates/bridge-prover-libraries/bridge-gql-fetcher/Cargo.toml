[package]
name = "bridge-gql-fetcher"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
hex = { workspace = true }
bincode = { workspace = true }
tokio = { workspace = true }
halo2-base = { workspace = true }
# Sole source of truth for the 16-leaf / depth-4 block-id Merkle constants —
# `BLOCK_ID_TREE_LEAF_COUNT` lives in `bridge_test_data_gen::layer_hashes`.
bridge-test-data-gen = { workspace = true }
metrics = "0.24"
