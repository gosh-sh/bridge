[package]
name = "deposit-chain-ids"
version = "0.2.0"
edition = "2021"
description = "Single source of truth for the EVM chains the deposit bridge accepts"
license = "Apache-2.0"

# Intentionally dependency-free: this crate is shared by three cargo trees
# (workspace, deposit-prover, deposit-relayer-daemon) and must stay trivial to
# vendor into any of them.
[dependencies]
